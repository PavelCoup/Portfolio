# In this role we use Grafana Collection for Ansible. Before using the Grafana collection, you need to install it with the Ansible Galaxy CLI:

```
ansible-galaxy collection install community.grafana
```


